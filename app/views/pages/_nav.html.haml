- if nav.content.start_with?('http')
  = link_to nav.title, nav.content, :class => 'dark', :target => :_blank
- elsif nav.content.start_with?('/')
  = link_to nav.title, nav.content, :class => 'dark'
- else
  = link_to nav.title, page_path(nav.key), :class => 'dark'
